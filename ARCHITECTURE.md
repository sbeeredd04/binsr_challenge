# TREC PDF Generator - System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      TREC PDF Generator                      │
│                                                              │
│  Input: inspection.json → Output: TREC_Report.pdf          │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Input      │    │  Processing  │    │   Output     │
│              │    │              │    │              │
│ inspection   │───▶│ TypeScript   │───▶│ Flattened    │
│   .json      │    │   Services   │    │   PDF        │
│              │    │              │    │              │
│ 18 sections  │    │ pdf-lib +    │    │ Submit-ready │
│ 139 items    │    │ qrcode       │    │ 6-30 pages   │
└──────────────┘    └──────────────┘    └──────────────┘
```

---

## 🏗️ Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              index.ts (Entry Point)                   │  │
│  │  • CLI argument parsing                               │  │
│  │  • Error handling                                     │  │
│  │  • User feedback                                      │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    ORCHESTRATION LAYER                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         TRECGenerator (Main Orchestrator)             │  │
│  │  • Coordinates all services                           │  │
│  │  • Manages workflow                                   │  │
│  │  • Validates input/output                             │  │
│  │  • Error handling                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                           │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  FormFiller  │  │ImageEmbedder │  │ QRGenerator  │      │
│  │              │  │              │  │              │      │
│  │ Fill header  │  │ Embed photos │  │ Generate QR  │      │
│  │ Check boxes  │  │ Scale images │  │ Add pages    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│                     TRANSFORMATION LAYER                     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │  DataMapper  │  │StatusMapper  │                         │
│  │              │  │              │                         │
│  │ JSON→Form    │  │ Status→Box   │                         │
│  │ Extract data │  │ Calculate    │                         │
│  └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│                       UTILITY LAYER                          │
│                                                              │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐                 │
│  │  Logger  │  │ Validator │  │FileUtils │                 │
│  │          │  │           │  │          │                 │
│  │ Logging  │  │ Validate  │  │ File I/O │                 │
│  └──────────┘  └───────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     CONFIGURATION LAYER                      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              constants.ts (Config)                    │  │
│  │  • Field mappings                                     │  │
│  │  • Status mappings                                    │  │
│  │  • Page configuration                                 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                       TYPE LAYER                             │
│                                                              │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │ inspection.ts    │  │    trec.ts       │                │
│  │                  │  │                  │                │
│  │ Input types      │  │ Output types     │                │
│  └──────────────────┘  └──────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
┌───────────────┐
│ inspection    │
│   .json       │ 
│               │
│ • 18 sections │
│ • 139 items   │
│ • Comments    │
│ • Media       │
└───────┬───────┘
        │
        ▼
┌───────────────────────────────┐
│  Validator                    │
│  ✓ Check structure            │
│  ✓ Validate required fields   │
└───────┬───────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│  DataMapper                   │
│  • Extract header data        │
│  • Flatten sections/items     │
│  • Collect media              │
│  • Sort by line number        │
└───────┬───────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│  TRECFormData                 │
│  {                            │
│    clientName: string,        │
│    propertyAddress: string,   │
│    items: TRECItem[],         │
│    ...                        │
│  }                            │
└───────┬───────────────────────┘
        │
        ├──────────────────────┐
        │                      │
        ▼                      ▼
┌───────────────┐    ┌───────────────┐
│  FormFiller   │    │ StatusMapper  │
│               │    │               │
│ Fill header   │◀───│ Map status    │
│ Check boxes   │    │ to checkbox   │
└───────┬───────┘    └───────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  TREC_Template_Blank.pdf            │
│  (Fields filled, boxes checked)     │
└───────┬─────────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│  ImageEmbedder                    │
│  • Download/read images           │
│  • Create pages                   │
│  • Scale & embed                  │
└───────┬───────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│  QRGenerator                      │
│  • Generate QR codes              │
│  • Create pages                   │
│  • Add instructions               │
└───────┬───────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│  Form.flatten()                   │
│  Convert to static content        │
└───────┬───────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│  PDFDocument.save()               │
│  Write to file                    │
└───────┬───────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│  output/                          │
│  TREC_Report_[date]_[time].pdf   │
│                                   │
│  ✓ All fields filled              │
│  ✓ Checkboxes checked             │
│  ✓ Images embedded                │
│  ✓ QR codes added                 │
│  ✓ Form flattened                 │
└───────────────────────────────────┘
```

---

## 🎯 Service Interaction Diagram

```
                    ┌──────────────┐
                    │TRECGenerator │
                    │  (Main)      │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ DataMapper   │   │  FormFiller  │   │ImageEmbedder │
└──────┬───────┘   └──────┬───────┘   └──────┬───────┘
       │                  │                  │
       │                  │                  │
       │           ┌──────▼───────┐          │
       └──────────▶│StatusMapper  │          │
                   └──────────────┘          │
                                            │
                           ┌────────────────┘
                           │
                    ┌──────▼───────┐
                    │ QRGenerator  │
                    └──────────────┘

                    ┌──────────────┐
                    │   Utilities  │
                    │              │
All Services ─────▶ │  • Logger    │
                    │  • Validator │
                    │  • FileUtils │
                    └──────────────┘

                    ┌──────────────┐
                    │ Configuration│
                    │              │
All Services ─────▶ │  constants   │
                    │              │
                    └──────────────┘

                    ┌──────────────┐
                    │    Types     │
                    │              │
All Services ─────▶ │  inspection  │
                    │  trec        │
                    └──────────────┘
```

---

## 📋 Component Responsibilities

### Entry Point Layer
```
index.ts
├── Parse CLI arguments
├── Load inspection.json
├── Call TRECGenerator
├── Display results
└── Handle errors
```

### Orchestration Layer
```
TRECGenerator
├── Step 1: Validate input
├── Step 2: Validate template
├── Step 3: Load PDF template
├── Step 4: Map data (DataMapper)
├── Step 5: Fill form (FormFiller)
├── Step 6: Embed images (ImageEmbedder)
├── Step 7: Generate QR codes (QRGenerator)
├── Step 8: Flatten form
├── Step 9: Ensure output directory
├── Step 10: Save PDF
└── Step 11: Validate output
```

### Service Layer
```
FormFiller
├── Fill header text fields
│   ├── CLIENT_NAME
│   ├── INSPECTOR_NAME
│   ├── PROPERTY_ADDRESS
│   └── INSPECTION_DATE
└── Check checkboxes
    ├── Calculate field name (StatusMapper)
    ├── Get checkbox
    └── Check it

ImageEmbedder
├── For each item with photos
│   ├── Create page
│   ├── Draw header
│   ├── Download/read image
│   ├── Embed image (JPG/PNG)
│   ├── Scale to fit
│   └── Add caption

QRGenerator
├── For each item with videos
│   ├── Create page
│   ├── Generate QR code
│   ├── Embed QR image
│   ├── Add item info
│   └── Add instructions
```

### Transformation Layer
```
DataMapper
├── getHeaderData()
│   └── Extract: client, inspector, property, date
├── getLineItems()
│   ├── Flatten sections
│   ├── Extract line items
│   ├── Collect media
│   └── Sort by number
└── getFormData()
    └── Combine header + items

StatusMapper
├── getCheckboxFieldName()
│   ├── Calculate page number
│   ├── Calculate index on page
│   ├── Calculate checkbox index
│   └── Build field name
└── getAllCheckboxes()
    └── Return all 4 checkbox names
```

### Utility Layer
```
Logger
├── info()
├── debug()
├── warn()
├── error()
└── success()

Validator
├── validateInspectionData()
├── validateTemplate()
└── validatePDF()

FileUtils
├── ensureDirectory()
├── generateOutputFilename()
├── readJSON()
├── fileExists()
└── getFileSizeMB()
```

---

## 🔢 Status to Checkbox Mapping Algorithm

```
Input: itemIndex, status

Step 1: Get checkbox offset from status
┌──────────┬────────────┐
│ Status   │ Offset     │
├──────────┼────────────┤
│ I        │ 0          │
│ NI       │ 1          │
│ NP       │ 2          │
│ D        │ 3          │
│ null     │ skip       │
└──────────┴────────────┘

Step 2: Calculate page
page = floor(itemIndex / 35) + 3
(35 items per page, starting at page 3)

Step 3: Calculate index on page
indexOnPage = itemIndex % 35

Step 4: Calculate absolute checkbox index
checkboxIndex = indexOnPage * 4 + offset
(4 checkboxes per item)

Step 5: Build field name
fieldName = topmostSubform[0].Page{page}[0].CheckBox1[{checkboxIndex}]

Example:
  itemIndex = 0, status = "I"
  → offset = 0
  → page = 0/35 + 3 = 3
  → indexOnPage = 0
  → checkboxIndex = 0*4 + 0 = 0
  → topmostSubform[0].Page3[0].CheckBox1[0]

  itemIndex = 37, status = "D"
  → offset = 3
  → page = 37/35 + 3 = 4
  → indexOnPage = 37 % 35 = 2
  → checkboxIndex = 2*4 + 3 = 11
  → topmostSubform[0].Page4[0].CheckBox1[11]
```

---

## 🎨 PDF Structure

```
┌─────────────────────────────────────┐
│  Page 1: Header                     │
│  ┌───────────────────────────────┐  │
│  │ Client Name:     [filled]     │  │
│  │ Client Email:    [filled]     │  │
│  │ Inspector Name:  [filled]     │  │
│  │ Property Addr:   [filled]     │  │
│  │ Inspection Date: [filled]     │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Page 2: Disclaimers                │
│  (Text fields filled)               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Pages 3-6: Inspection Items        │
│  ┌───────────────────────────────┐  │
│  │ Item 1: [Title]               │  │
│  │   ☑ I  ☐ NI  ☐ NP  ☐ D       │  │
│  │                               │  │
│  │ Item 2: [Title]               │  │
│  │   ☐ I  ☐ NI  ☐ NP  ☑ D       │  │
│  │                               │  │
│  │ ... (35 items per page)       │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Page 7+: Images (if any)           │
│  ┌───────────────────────────────┐  │
│  │ Item 5: Kitchen Sink          │  │
│  │ Section: Plumbing             │  │
│  │                               │  │
│  │  [Embedded Image]             │  │
│  │  Caption: Leak detected       │  │
│  │                               │  │
│  │  [Embedded Image]             │  │
│  │  Caption: Close-up            │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Page N+: QR Codes (if any)         │
│  ┌───────────────────────────────┐  │
│  │ Item 12: Roof Inspection      │  │
│  │ Video Link:                   │  │
│  │                               │  │
│  │       ┌─────────────┐         │  │
│  │       │ QR CODE     │         │  │
│  │       │             │         │  │
│  │       └─────────────┘         │  │
│  │                               │  │
│  │ Scan to view video            │  │
│  │ URL: https://...              │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 🔐 Type Safety Flow

```
inspection.json (unknown)
    │
    ├─ readJSON<InspectionData>()
    │
    ▼
InspectionData (typed)
    │
    ├─ new DataMapper(inspectionData)
    │
    ▼
TRECFormData (typed)
    │
    ├─ formFiller.fillForm(formData)
    │
    ▼
PDF Form Fields (filled)
    │
    ├─ form.flatten()
    │
    ▼
Static PDF Content
```

---

## ⚡ Performance Characteristics

```
┌────────────────────────┬──────────┬─────────────┐
│ Operation              │ Time     │ Complexity  │
├────────────────────────┼──────────┼─────────────┤
│ Load template          │ ~100ms   │ O(1)        │
│ Parse JSON             │ ~50ms    │ O(n)        │
│ Map data               │ ~10ms    │ O(n)        │
│ Fill form fields       │ ~100ms   │ O(n)        │
│ Embed image (each)     │ ~200ms   │ O(1)        │
│ Generate QR (each)     │ ~50ms    │ O(1)        │
│ Flatten form           │ ~500ms   │ O(n*m)      │
│ Save PDF               │ ~200ms   │ O(1)        │
├────────────────────────┼──────────┼─────────────┤
│ Total (no media)       │ ~1s      │             │
│ Total (with 10 images) │ ~3-4s    │             │
└────────────────────────┴──────────┴─────────────┘

n = number of line items
m = number of form fields
```

---

## 📦 Dependency Graph

```
pdf-lib (^1.17.1)
├── @pdf-lib/standard-fonts
├── @pdf-lib/upng
└── pako

qrcode (^1.5.3)
├── dijkstrajs
└── pngjs

typescript (^5.0.0) [devDependency]

@types/node (^20.0.0) [devDependency]

@types/qrcode (^1.5.5) [devDependency]

ts-node (^10.9.0) [devDependency]
├── @cspotcode/source-map-support
└── acorn
```

---

## 🎯 Extension Points

### Adding New Field Mappings
```typescript
// src/config/constants.ts
export const FORM_FIELDS = {
  // Add new field
  NEW_FIELD: 'topmostSubform[0].Page1[0].NewField[0]',
}

// src/services/FormFiller.ts
// Add to fieldMappings
fieldMappings[FORM_FIELDS.NEW_FIELD] = formData.newField;
```

### Adding New Status Types
```typescript
// src/types/inspection.ts
export type InspectionStatus = 
  | 'I' | 'NI' | 'NP' | 'D'
  | 'NEW_STATUS'  // Add new status

// src/config/constants.ts
export const STATUS_TO_CHECKBOX = {
  'NEW_STATUS': 4,  // Add mapping
}
```

### Adding New Services
```typescript
// src/services/NewService.ts
export class NewService {
  constructor(private pdfDoc: PDFDocument) {}
  
  async process(data: TRECItem[]): Promise<void> {
    // Implementation
  }
}

// src/services/TRECGenerator.ts
const newService = new NewService(pdfDoc);
await newService.process(formData.items);
```

---

## 📚 Module Dependencies

```
index.ts
  └── services/TRECGenerator
      ├── mappers/DataMapper
      │   └── types/inspection
      ├── services/FormFiller
      │   ├── mappers/StatusMapper
      │   └── types/trec
      ├── services/ImageEmbedder
      │   └── types/trec
      └── services/QRGenerator
          └── types/trec

All modules depend on:
  ├── utils/logger
  ├── utils/validator
  ├── utils/fileUtils
  └── config/constants
```

---

**Architecture designed for**:
- ✅ Maintainability (clear separation)
- ✅ Testability (modular services)
- ✅ Extensibility (easy to add features)
- ✅ Type Safety (full TypeScript)
- ✅ Performance (efficient algorithms)
- ✅ Reliability (error handling)

---

**Document Version**: 1.0  
**Date**: November 3, 2025  
**Status**: Complete and Production-Ready

